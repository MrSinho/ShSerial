language: c
compiler: gcc

os: linux
dist: xenial
sudo: require

before_script:
  - sudo apt-get install -y gcc cmake make
  - mkdir build
  - cd build
  - cmake -DFGG_SERIAL_BUILD_EXAMPLE=ON ..

script:
  - make
